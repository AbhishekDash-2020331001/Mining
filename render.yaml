services:
  - type: web
    name: mining-prediction-api
    env: python
    runtime: python-3.12.0
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
